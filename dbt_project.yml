
name: 'projet_sid'
version: '1.0.0'

profile: 'projet_sid'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:        
  - "target"
  - "dbt_packages"



models:
  projet_sid:
    
    staging:
      +materialized: table
      +schema: staging

    dimensions:
      +materialized: table
      +schema: dimensions

    facts:
      +materialized: table
      +schema: facts

    bridge:
      +materialized: table
      +schema: bridge  
    
